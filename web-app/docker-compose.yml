version: "3.8"

services:
  readalong-server:
    build:
      context: ../rust-core
      dockerfile: ../web-app/Dockerfile
    container_name: readalong-server
    ports:
      - "3000:3000"
    volumes:
      - readalong_data:/app/data
    restart: unless-stopped
    environment:
      - API_TOKEN=${API_TOKEN}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      - readalong-server

volumes:
  readalong_data: